<Window x:Class="TempleLampSystem.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:TempleLampSystem.Views"
        Title="點燈管理系統"
        Height="800"
        Width="1280"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 頂部標題區 -->
        <Border Grid.Row="0"
                Background="{StaticResource SecondaryBrush}"
                Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 系統標題 -->
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="☯"
                               FontSize="32"
                               Foreground="#FFD700"
                               VerticalAlignment="Center"
                               Margin="0,0,15,0"/>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="點燈管理系統"
                                   FontSize="24"
                                   FontWeight="Bold"
                                   Foreground="White"
                                   FontFamily="{StaticResource PrimaryFont}"/>
                        <TextBlock Text="Temple Lamp Management System"
                                   FontSize="11"
                                   Foreground="#D4A574"
                                   FontFamily="{StaticResource PrimaryFont}"/>
                    </StackPanel>
                </StackPanel>

                <!-- 宮廟名稱 -->
                <TextBlock Grid.Column="2"
                           Text="鳳屏宮"
                           FontSize="20"
                           FontWeight="SemiBold"
                           Foreground="#FFD700"
                           VerticalAlignment="Center"
                           FontFamily="{StaticResource PrimaryFont}"/>
            </Grid>
        </Border>

        <!-- 主內容區 -->
        <Grid Grid.Row="1" Margin="15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 左側：客戶搜尋 -->
            <GroupBox Grid.Column="0" Header="客戶搜尋" Margin="5">
                <views:CustomerSearchView x:Name="CustomerSearchView"/>
            </GroupBox>

            <!-- 分隔線 -->
            <GridSplitter Grid.Column="1"
                          Width="8"
                          HorizontalAlignment="Stretch"
                          Background="Transparent"/>

            <!-- 右側：點燈操作 -->
            <GroupBox Grid.Column="2" Header="點燈操作" Margin="5">
                <views:LampOrderView x:Name="LampOrderView"/>
            </GroupBox>
        </Grid>

        <!-- 底部狀態列 -->
        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <Border x:Name="OnlineIndicatorBorder"
                            Width="12" Height="12"
                            CornerRadius="6"
                            Background="Gray"
                            Margin="0,0,8,0"/>
                    <Ellipse x:Name="OnlineIndicator"
                             Width="0" Height="0"
                             Visibility="Collapsed"/>
                    <TextBlock x:Name="SyncStatusText" Text="檢查連線中..." FontWeight="SemiBold"/>
                </StackPanel>
            </StatusBarItem>
            <Separator Background="#D4A574"/>
            <StatusBarItem>
                <TextBlock x:Name="PendingSyncText" Text=""/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="時間：" FontSize="13"/>
                    <TextBlock x:Name="CurrentTimeText" Text="" FontWeight="SemiBold"/>
                </StackPanel>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
